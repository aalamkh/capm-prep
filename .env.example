# Local development uses SQLite (zero setup).
DATABASE_URL="file:./dev.db"

# Production: switch to Postgres. The vercel-build script auto-detects the
# scheme and rewrites prisma/schema.prisma's provider line accordingly.
# DATABASE_URL="postgresql://user:pass@host:5432/db?sslmode=require"
